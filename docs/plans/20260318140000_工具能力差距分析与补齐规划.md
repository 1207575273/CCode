# 工具能力差距分析与补齐规划

> 日期: 2026-03-18
> 数据来源: `@anthropic-ai/claude-code/sdk-tools.d.ts`（Claude Code CLI 官方工具定义）
> 状态: 分析完成，待实施

---

## 一、Claude Code CLI 完整工具清单

基于 `sdk-tools.d.ts` 逆向分析，Claude Code CLI 共有 **22 个工具**，按职责分为 7 类：

### 1.1 文件操作（5 个）

| 工具 | 参数 | 说明 |
|------|------|------|
| **FileRead** | `file_path, offset, limit, pages` | 读文件，支持图片/PDF/Notebook |
| **FileWrite** | `file_path, content` | 写文件（覆盖） |
| **FileEdit** | `file_path, old_string, new_string, replace_all` | 精确替换 |
| **Glob** | `pattern, path` | 文件名匹配 |
| **Grep** | `pattern, path, type, glob, output_mode` | 内容搜索 |

### 1.2 Shell 执行（3 个）

| 工具 | 参数 | 说明 |
|------|------|------|
| **Bash** | `command, cwd, timeout, run_in_background` | 执行 Shell 命令 |
| **TaskOutput** | `task_id, block, timeout` | 读取后台任务输出 |
| **TaskStop** | `task_id` | 停止后台任务 |

### 1.3 Agent 派发（1 个）

| 工具 | 参数 | 说明 |
|------|------|------|
| **Agent** | `description, prompt, subagent_type, model, run_in_background, name, mode, isolation, team_name` | 派发子 Agent，支持后台运行、git worktree 隔离、权限模式选择 |

### 1.4 任务规划（1 个）

| 工具 | 参数 | 说明 |
|------|------|------|
| **TodoWrite** | `todos: [{content, status, activeForm}]` | 全量覆盖写 TODO 列表，三态（pending/in_progress/completed） |

### 1.5 Web 能力（2 个）

| 工具 | 参数 | 说明 |
|------|------|------|
| **WebFetch** | `url, prompt` | 抓取网页内容（带 prompt 指引提取） |
| **WebSearch** | `query, allowed_domains, blocked_domains` | 网页搜索 |

### 1.6 MCP 与资源（6 个）

| 工具 | 参数 | 说明 |
|------|------|------|
| **McpInput** | 动态 | MCP 工具调用（动态注册） |
| **ListMcpResources** | `server?` | 列出 MCP 资源 |
| **ReadMcpResource** | `server, uri` | 读取 MCP 资源 |
| **SubscribeMcpResource** | `server, uri` | 订阅 MCP 资源变更 |
| **UnsubscribeMcpResource** | `server, subscriptionId` | 取消订阅 |
| **SubscribePolling** | `type, server, toolName, uri, intervalMs, reason` | 轮询订阅 MCP 工具/资源 |

### 1.7 其他（4 个）

| 工具 | 参数 | 说明 |
|------|------|------|
| **AskUserQuestion** | `questions[{question, label, options, type}]` | 用户问答表单 |
| **NotebookEdit** | `notebook_path, cell_id, new_source, cell_type` | Jupyter Notebook 编辑 |
| **Config** | `setting, value?` | 运行时配置读写 |
| **EnterWorktree / ExitWorktree** | `name? / action, discard_changes?` | Git Worktree 隔离环境 |
| **ExitPlanMode** | `allowedPrompts?` | 退出计划模式，声明权限 |

---

## 二、ZCli 当前工具清单

### 2.1 原子工具（7 个）

| 工具 | 对标 Claude Code | 状态 |
|------|-----------------|------|
| `read_file` | FileRead | ✅ 功能等价（不支持图片/PDF） |
| `write_file` | FileWrite | ✅ |
| `edit_file` | FileEdit | ✅ |
| `glob` | Glob | ✅ |
| `grep` | Grep | ✅ |
| `bash` | Bash | ✅ 含 `run_in_background` |
| `kill_shell` | TaskStop | ✅ 功能等价 |

### 2.2 扩展工具（3 个）

| 工具 | 对标 Claude Code | 状态 |
|------|-----------------|------|
| `dispatch_agent` | Agent | ⚡ 基础功能有，缺后台运行/worktree/模型选择 |
| `ask_user_question` | AskUserQuestion | ✅ |
| `skill` | 无 | ZCli 独有 |

### 2.3 MCP 动态工具

通过 MCP 协议动态注册的外部工具（mysql, puppeteer, web-search 等），等价于 Claude Code 的 McpInput。

---

## 三、差距分析

### 3.1 完全缺失的能力（红色）

| 缺失 | Claude Code 工具 | 影响 | 优先级 |
|------|-----------------|------|--------|
| **任务规划可视化** | TodoWrite | Agent 无法显式展示任务分解计划，用户看不到"接下来要做什么" | **P0** |
| **后台任务输出读取** | TaskOutput | bash run_in_background 启动的任务无法读取实时输出 | **P1** |
| **图片/PDF 读取** | FileRead (image/pdf) | 无法处理截图、设计稿、PDF 文档 | P2 |

### 3.2 功能不完整的能力（黄色）

| 差距 | Claude Code 能力 | ZCli 当前 | 优先级 |
|------|-----------------|----------|--------|
| **Agent 后台运行** | `run_in_background: true` + `TaskOutput` 读取 | 不支持 | **P1** |
| **Agent 模型选择** | `model: "sonnet" / "opus" / "haiku"` | 子 Agent 继承父 Agent 模型 | P2 |
| **Agent Worktree 隔离** | `isolation: "worktree"` | 不支持 | P2 |
| **Agent 权限模式** | `mode: "acceptEdits" / "bypassPermissions" / "plan"` | 主 Agent 派发即授权 | P2 |

### 3.3 不需要补的能力

| Claude Code 工具 | 理由 |
|-----------------|------|
| WebFetch / WebSearch | 已通过 MCP 扩展覆盖（web-search-prime, deepwiki） |
| NotebookEdit | Jupyter 场景非核心（Python 生态可通过 MCP 扩展） |
| Config | 已有 Web Dashboard 设置管理 + `/model` 指令 |
| EnterWorktree / ExitWorktree | P2，后续与 Agent 隔离一起做 |
| ExitPlanMode | 与 TodoWrite 一起做 |
| SubscribePolling | MCP 高级能力，非核心 |
| MCP 资源管理 | 当前 MCP 工具调用已覆盖核心场景 |

---

## 四、补齐规划

### 4.1 Phase 1: TodoWrite — 任务规划工具（P0）

**目标**：让 Agent 能显式创建和管理任务计划，CLI 和 Web 都能展示任务列表。

**Claude Code 的 TodoWrite**：

```typescript
interface TodoWriteInput {
  todos: {
    content: string                                    // 任务描述
    status: "pending" | "in_progress" | "completed"   // 三态
    activeForm: string                                 // 当前活跃表单
  }[]
}

interface TodoWriteOutput {
  oldTodos: Todo[]     // 更新前
  newTodos: Todo[]     // 更新后
  verificationNudgeNeeded?: boolean
}
```

**ZCli 的 TodoWrite 设计**：

```typescript
// src/tools/todo-write.ts

interface TodoItem {
  id: string
  content: string
  status: 'pending' | 'in_progress' | 'completed'
}

interface TodoWriteArgs {
  todos: TodoItem[]  // 全量覆盖
}
```

**存储**：内存中维护，不持久化（session 级生命周期）。通过 EventBus 广播状态变更，CLI 和 Web 实时渲染。

**CLI 渲染**：

```
📋 任务计划 (2/5 完成)
  ✅ 1. 读取配置文件
  ✅ 2. 分析当前架构
  🔄 3. 编写新模块          ← 当前
  ⬜ 4. 编写测试
  ⬜ 5. 更新文档
```

**Web 渲染**：进度条 + checkbox 列表，与 CLI 实时同步。

**新增事件**：

```typescript
// AgentEvent
| { type: 'todo_update'; todos: TodoItem[] }

// BridgeEvent
| { type: 'todo_update'; todos: TodoItem[] }
```

**改动文件**：

| 文件 | 改动 |
|------|------|
| `src/tools/todo-write.ts` | 新增工具 |
| `src/core/agent-loop.ts` | AgentEvent 加 `todo_update` |
| `src/core/bootstrap.ts` | 注册 TodoWriteTool |
| `src/ui/useChat.ts` | 消费 `todo_update` 事件，维护 todos 状态 |
| `src/ui/TodoPanel.tsx` | 新增 CLI 渲染组件 |
| `src/ui/App.tsx` | 显示 TodoPanel |
| `web/src/types.ts` | 加 `todo_update` 事件 |
| `web/src/pages/ChatPage.tsx` | 渲染任务列表 |
| `src/core/event-bus.ts` | BridgeEvent 加 `todo_update` |
| `src/server/bridge/client.ts` | 透传 `todo_update` |

**预估工作量**：3-4 小时

### 4.2 Phase 2: TaskOutput — 后台任务输出读取（P1）

**目标**：补全 bash `run_in_background` 的闭环——启动后台任务后可以读取其输出。

**Claude Code 的 TaskOutput**：

```typescript
interface TaskOutputInput {
  task_id: string     // 任务 ID
  block: boolean      // 是否阻塞等待
  timeout: number     // 最大等待时间(ms)
}
```

**ZCli 设计**：

```typescript
// src/tools/task-output.ts

interface TaskOutputArgs {
  pid: number          // 进程 PID（复用 process-tracker 的 ID）
  block?: boolean      // 是否等待完成
  timeout?: number     // 超时(ms)
}
```

**核心改动**：
- `process-tracker.ts` 需要改造——后台进程不能用 `stdio: 'ignore'`，要用管道捕获 stdout/stderr
- 新增 `task-output.ts` 工具——从 tracker 读取已捕获的输出

**预估工作量**：2-3 小时

### 4.3 Phase 3: Agent 能力增强（P2）

| 能力 | 改动 | 工作量 |
|------|------|--------|
| 模型选择 | dispatch_agent 加 `model` 参数，子 Agent 可指定不同模型 | 1h |
| 后台运行 | dispatch_agent 加 `run_in_background`，复用 TaskOutput 读取结果 | 2h |
| Worktree 隔离 | 新增 git worktree 管理模块 | 4h |
| 权限模式 | 子 Agent 支持 `mode` 参数控制权限策略 | 2h |

### 4.4 Phase 4: 图片/PDF 读取（P2）

- FileRead 支持图片返回 base64
- FileRead 支持 PDF 文本提取
- 需要新增依赖（pdf-parse 或 pdfjs-dist）

---

## 五、工具能力对照表（完整版）

| # | Claude Code | ZCli | 状态 | 优先级 |
|---|------------|------|------|--------|
| 1 | Bash | bash | ✅ 等价 | — |
| 2 | FileRead | read_file | ⚡ 缺图片/PDF | P2 |
| 3 | FileWrite | write_file | ✅ 等价 | — |
| 4 | FileEdit | edit_file | ✅ 等价 | — |
| 5 | Glob | glob | ✅ 等价 | — |
| 6 | Grep | grep | ✅ 等价 | — |
| 7 | Agent | dispatch_agent | ⚡ 缺后台/worktree/模型选择 | P1-P2 |
| 8 | AskUserQuestion | ask_user_question | ✅ 等价 | — |
| 9 | TaskStop | kill_shell | ✅ 等价 | — |
| 10 | **TodoWrite** | **—** | **❌ 缺失** | **P0** |
| 11 | **TaskOutput** | **—** | **❌ 缺失** | **P1** |
| 12 | WebFetch | MCP 扩展覆盖 | ✅ | — |
| 13 | WebSearch | MCP 扩展覆盖 | ✅ | — |
| 14 | McpInput | MCP 动态注册 | ✅ 等价 | — |
| 15 | NotebookEdit | — | 🔲 非核心 | P3 |
| 16 | Config | Web Dashboard | ✅ 替代方案 | — |
| 17 | EnterWorktree | — | 🔲 | P2 |
| 18 | ExitWorktree | — | 🔲 | P2 |
| 19 | ExitPlanMode | — | 🔲 与 TodoWrite 一起 | P0 |
| 20 | ListMcpResources | — | 🔲 非核心 | P3 |
| 21 | SubscribePolling | — | 🔲 非核心 | P3 |
| — | — | skill | ZCli 独有 | — |
| — | — | kill_shell (进程追踪) | ZCli 独有 | — |

**统计**：
- ✅ 等价或已覆盖：13 个
- ⚡ 部分缺失：2 个（FileRead 图片/PDF、Agent 高级能力）
- ❌ 完全缺失需补：2 个（TodoWrite、TaskOutput）
- 🔲 暂不做：5 个
- ZCli 独有：2 个

---

## 六、实施路线

```
Phase 1 (P0): TodoWrite — 任务规划工具
  → Agent 显式创建任务计划
  → CLI checkbox 列表 + Web 进度条
  → 预估 3-4h

Phase 2 (P1): TaskOutput — 后台任务输出
  → 补全 bash run_in_background 闭环
  → process-tracker 改造（管道捕获输出）
  → 预估 2-3h

Phase 3 (P2): Agent 增强
  → 模型选择 + 后台运行 + Worktree
  → 预估 9h（分 3 批）

Phase 4 (P2): FileRead 增强
  → 图片 base64 + PDF 文本
  → 预估 2-3h
```

**Phase 1 是当前最高优先级——直接补齐 Agent 架构的 Planning 短板。**
