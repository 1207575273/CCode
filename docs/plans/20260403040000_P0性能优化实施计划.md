# P0 性能优化实施计划：resolveShell + detectPlatform 缓存

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 `detectPlatform()` 和 `resolveShell()` 加模块级缓存，消除每次 bash 工具调用时的重复平台检测和文件系统遍历

**Architecture:** 两个纯函数各自加一个模块级缓存变量（惰性初始化），首次调用计算并缓存，后续调用直接返回缓存值

**Tech Stack:** TypeScript, Vitest

---

### Task 1: detectPlatform() 加缓存

**Files:**
- Modify: `cCli/src/platform/detector.ts:18-30`
- Modify: `cCli/tests/unit/platform/detector.test.ts`

- [ ] **Step 1: 补充缓存行为的测试**

在 `tests/unit/platform/detector.test.ts` 末尾追加：

```typescript
it('多次调用返回同一引用（缓存生效）', () => {
  const a = detectPlatform()
  const b = detectPlatform()
  expect(a).toBe(b)  // 严格引用相等，证明是缓存
})
```

- [ ] **Step 2: 运行测试，确认新测试失败**

运行：`cd cCli && npx vitest run tests/unit/platform/detector.test.ts --reporter=verbose`

预期：新测试失败（当前每次返回新对象，`toBe` 引用不等）

- [ ] **Step 3: 实现缓存**

修改 `src/platform/detector.ts`，将 `detectPlatform()` 改为惰性缓存：

```typescript
let _cached: PlatformInfo | undefined

export function detectPlatform(): PlatformInfo {
  if (_cached) return _cached
  const raw = os.platform()
  const platform: Platform = SUPPORTED_PLATFORMS.has(raw) ? (raw as Platform) : 'linux'
  _cached = {
    platform,
    isWindows: platform === 'win32',
    isLinux: platform === 'linux',
    isMac: platform === 'darwin',
    arch: os.arch(),
    homeDir: os.homedir(),
    ccodeDir: `${os.homedir()}/.ccode`,
  }
  return _cached
}
```

- [ ] **Step 4: 运行测试，确认全部通过**

运行：`cd cCli && npx vitest run tests/unit/platform/detector.test.ts --reporter=verbose`

预期：3 个测试全部 PASS

- [ ] **Step 5: 提交**

```bash
cd cCli
git add src/platform/detector.ts tests/unit/platform/detector.test.ts
git commit -m "perf: detectPlatform() 加模块级缓存，避免重复 os 调用"
```

---

### Task 2: resolveShell() 加缓存

**Files:**
- Modify: `cCli/src/platform/shell-resolver.ts:33-53`
- Modify: `cCli/tests/unit/platform/shell-resolver.test.ts`

- [ ] **Step 1: 补充缓存行为的测试**

在 `tests/unit/platform/shell-resolver.test.ts` 末尾追加：

```typescript
it('多次调用返回同一引用（缓存生效）', () => {
  const a = resolveShell()
  const b = resolveShell()
  expect(a).toBe(b)  // 严格引用相等，证明是缓存
})
```

- [ ] **Step 2: 运行测试，确认新测试失败**

运行：`cd cCli && npx vitest run tests/unit/platform/shell-resolver.test.ts --reporter=verbose`

预期：新测试失败

- [ ] **Step 3: 实现缓存**

修改 `src/platform/shell-resolver.ts`，在 `resolveShell()` 上加缓存：

将原 `resolveShell` 函数体重命名为 `doResolveShell`，新的 `resolveShell` 做缓存包装：

```typescript
let _cached: ResolvedShell | undefined

function doResolveShell(preferred?: ShellType): ResolvedShell {
  const { isWindows } = detectPlatform()

  if (isWindows) {
    if (preferred !== 'powershell') {
      const gitBash = findGitBash()
      if (gitBash) {
        return { type: 'gitbash', path: gitBash, args: ['-c'] }
      }
    }
    return {
      type: 'powershell',
      path: 'powershell.exe',
      args: ['-NoProfile', '-NonInteractive', '-Command'],
    }
  }

  // Linux / macOS
  const bash = existsSync('/bin/bash') ? '/bin/bash' : '/bin/sh'
  return { type: bash.endsWith('bash') ? 'bash' : 'sh', path: bash, args: ['-c'] }
}

export function resolveShell(preferred?: ShellType): ResolvedShell {
  if (!_cached) _cached = doResolveShell(preferred)
  return _cached
}
```

- [ ] **Step 4: 运行测试，确认全部通过**

运行：`cd cCli && npx vitest run tests/unit/platform/shell-resolver.test.ts --reporter=verbose`

预期：3 个测试全部 PASS

- [ ] **Step 5: 运行全量 platform 测试，确认无回归**

运行：`cd cCli && npx vitest run tests/unit/platform/ --reporter=verbose`

预期：所有 platform 测试 PASS

- [ ] **Step 6: 提交**

```bash
cd cCli
git add src/platform/shell-resolver.ts tests/unit/platform/shell-resolver.test.ts
git commit -m "perf: resolveShell() 加模块级缓存，避免每次 bash 调用都遍历文件系统"
```
