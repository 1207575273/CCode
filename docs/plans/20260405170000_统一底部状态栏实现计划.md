# 统一底部状态栏（StatusBar）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 token/context/cost 展示与新增的 MEM/CPU/运行时间合并为 InputBar 下方的一行固定状态栏

**Architecture:** 新增 `useStatusBar` hook（定时采集系统资源 + 订阅 token/context 变化）和 `StatusBar.tsx` 纯展示组件。移除 App.tsx 中现有的内联 token/context 展示节点。`session_resume` / `session_end` 事件新增 `accumulatedMs` 字段实现跨 resume 时间累加。`config.json` 新增 `statusBar` 布尔开关。

**Tech Stack:** React/Ink、Node.js `process.memoryUsage()` / `process.cpuUsage()` / `os` 模块

**设计文档:** `docs/plans/20260405160000_统一底部状态栏设计.md`

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `src/ui/StatusBar.tsx` | 新建 | 纯展示组件，渲染一行状态栏 |
| `src/ui/useStatusBar.ts` | 新建 | Hook：定时采集 MEM/CPU，计算运行时间，读取 token/context |
| `src/config/config-manager.ts` | 修改 | `CCodeConfig` 新增 `statusBar?: boolean` |
| `src/persistence/session-types.ts` | 修改 | `SessionEvent` 新增 `accumulatedMs?: number` |
| `src/observability/session-logger.ts` | 修改 | `finalize()` 写入 `accumulatedMs`，新增 `setAccumulatedMs()` |
| `src/ui/useChat.ts` | 修改 | `loadSession()` 提取历史 `accumulatedMs`，暴露给 App |
| `src/ui/App.tsx` | 修改 | 移除内联 token/context 展示，插入 `<StatusBar />` |
| `tests/unit/useStatusBar.test.ts` | 新建 | Hook 逻辑单元测试 |
| `tests/unit/StatusBar.test.ts` | 新建 | 渲染快照测试 |

---

### Task 1: 配置层 — CCodeConfig 新增 statusBar 开关

**Files:**
- Modify: `src/config/config-manager.ts:14-17`

- [ ] **Step 1: 在 CCodeConfig 接口添加 statusBar 字段**

```typescript
// src/config/config-manager.ts:14-18
export interface CCodeConfig {
  defaultProvider: string
  defaultModel: string
  providers: Record<string, ProviderConfig | undefined>
  statusBar?: boolean  // 新增，缺省 = true
}
```

在 `CCodeConfig` 接口的 `providers` 字段后面添加 `statusBar?: boolean`。

- [ ] **Step 2: 验证编译通过**

Run: `cd cCli && npx tsc --noEmit`
Expected: 无新增错误（原有 CommandSuggestion 错误忽略）

- [ ] **Step 3: 提交**

```bash
git add cCli/src/config/config-manager.ts
git commit -m "feat(config): CCodeConfig 新增 statusBar 布尔开关"
```

---

### Task 2: 持久化层 — SessionEvent 新增 accumulatedMs

**Files:**
- Modify: `src/persistence/session-types.ts:112-121`

- [ ] **Step 1: 在 SessionEvent 添加 accumulatedMs 字段**

在 `session-types.ts` 的 `/** 会话汇总 (session_end) */` 注释块中，`totalDurationMs` 之后添加：

```typescript
  /** 累计运行时长 ms（跨 resume 累加） */
  accumulatedMs?: number
```

最终该区域代码：

```typescript
  /** 会话汇总 (session_end) */
  totalInputTokens?: number
  totalOutputTokens?: number
  totalCacheReadTokens?: number
  totalCacheWriteTokens?: number
  totalToolCalls?: number
  totalLlmCalls?: number
  totalErrors?: number
  totalDurationMs?: number
  /** 累计运行时长 ms（跨 resume 累加） */
  accumulatedMs?: number
```

- [ ] **Step 2: 验证编译通过**

Run: `cd cCli && npx tsc --noEmit`
Expected: 无新增错误

- [ ] **Step 3: 提交**

```bash
git add cCli/src/persistence/session-types.ts
git commit -m "feat(persistence): SessionEvent 新增 accumulatedMs 字段"
```

---

### Task 3: 观测层 — SessionLogger 支持 accumulatedMs

**Files:**
- Modify: `src/observability/session-logger.ts`

- [ ] **Step 1: 添加 #accumulatedMs 私有字段和 setter**

在 `SessionLogger` 类中（`#cwd` 字段之后）添加：

```typescript
  #accumulatedMs: number = 0
```

在 `bind()` 方法之后添加 setter：

```typescript
  /** 设置历史累计时长（resume 时调用） */
  setAccumulatedMs(ms: number): void {
    this.#accumulatedMs = ms
  }

  /** 获取历史累计时长 */
  get accumulatedMs(): number {
    return this.#accumulatedMs
  }
```

- [ ] **Step 2: 修改 finalize() 写入 accumulatedMs**

修改 `finalize()` 方法，在 `session_end` 事件中追加 `accumulatedMs`：

```typescript
  /** 写入 session_end 汇总事件 */
  finalize(): void {
    if (!this.#sessionId) return
    const durationMs = Date.now() - this.#turnStats.startTime
    this.#appendEvent('session_end', {
      totalInputTokens: this.#turnStats.totalInputTokens,
      totalOutputTokens: this.#turnStats.totalOutputTokens,
      totalCacheReadTokens: this.#turnStats.totalCacheReadTokens,
      totalCacheWriteTokens: this.#turnStats.totalCacheWriteTokens,
      totalToolCalls: this.#turnStats.totalToolCalls,
      totalLlmCalls: this.#turnStats.totalLlmCalls,
      totalErrors: this.#turnStats.totalErrors,
      totalDurationMs: durationMs,
      accumulatedMs: this.#accumulatedMs + durationMs,
    })
  }
```

关键变化：`accumulatedMs: this.#accumulatedMs + durationMs`——把历史累计 + 本次运行写入，供下次 resume 读取。

- [ ] **Step 3: 验证编译通过**

Run: `cd cCli && npx tsc --noEmit`
Expected: 无新增错误

- [ ] **Step 4: 提交**

```bash
git add cCli/src/observability/session-logger.ts
git commit -m "feat(observability): SessionLogger 支持 accumulatedMs 跨 resume 累加"
```

---

### Task 4: useChat — resume 时提取 accumulatedMs

**Files:**
- Modify: `src/ui/useChat.ts`

- [ ] **Step 1: 在 useChat 中新增 accumulatedMs 状态**

在 `useChat` 函数内部（现有 `contextState` 状态附近）添加：

```typescript
const [accumulatedMs, setAccumulatedMs] = useState(0)
const [sessionStartTime] = useState(() => Date.now())
```

在 `UseChatReturn` 接口中添加：

```typescript
  /** 历史累计运行时长 ms（跨 resume） */
  accumulatedMs: number
  /** 本次 session 启动时间戳 */
  sessionStartTime: number
```

在 return 对象中暴露这两个值。

- [ ] **Step 2: 修改 loadSession() 提取 accumulatedMs**

在 `loadSession` 回调中，写入 `session_resume` 事件之前，遍历 JSONL 事件提取 `accumulatedMs`：

```typescript
  const loadSession = useCallback((sessionId: string, leafEventUuid?: string): void => {
    try {
      const snapshot = sessionStore.loadMessages(sessionId, leafEventUuid)

      // ── 提取历史累计时长 ──
      // 从 JSONL 中读取最后一个 session_end 事件的 accumulatedMs
      // loadMessages 内部已经遍历了事件链，这里用 store.getLastAccumulatedMs
      const lastAccMs = sessionStore.getLastAccumulatedMs(sessionId)
      setAccumulatedMs(lastAccMs)
      sessionLogger.setAccumulatedMs(lastAccMs)

      // ... 原有恢复逻辑不变 ...
```

- [ ] **Step 3: 在 SessionStore 添加 getLastAccumulatedMs 方法**

Modify: `src/persistence/session-store.ts`

在 `SessionStore` 类中添加：

```typescript
  /**
   * 从 JSONL 中提取最后一个 session_end 事件的 accumulatedMs。
   * 用于 resume 时恢复累计运行时长。
   * 找不到则返回 0。
   */
  getLastAccumulatedMs(sessionId: string): number {
    try {
      const filePath = this.#resolveSessionFile(sessionId)
      if (!filePath) return 0
      const content = readFileSync(filePath, 'utf-8')
      const lines = content.trim().split('\n').filter(Boolean)

      // 从尾部向前找第一个 session_end
      for (let i = lines.length - 1; i >= 0; i--) {
        try {
          const event = JSON.parse(lines[i]) as SessionEvent
          if (event.type === 'session_end' && event.accumulatedMs != null) {
            return event.accumulatedMs
          }
        } catch { continue }
      }
      return 0
    } catch {
      return 0
    }
  }
```

注意：需要一个 `#resolveSessionFile` 辅助方法来根据 sessionId 找到 JSONL 文件路径。检查 `SessionStore` 中是否已有类似方法（`loadMessages` 内部有路径查找逻辑），如果有则复用，如果没有则抽取。

- [ ] **Step 4: 修改 session_resume 事件写入 accumulatedMs**

在 `loadSession` 中写入 `session_resume` 事件时追加 `accumulatedMs`：

```typescript
      sessionStore.append(sessionId, {
        sessionId,
        type: 'session_resume',
        timestamp: new Date().toISOString(),
        uuid: resumeEventId,
        parentUuid: sessionLogger.lastEventUuid,
        cwd: process.cwd(),
        provider: snapshot.provider,
        model: snapshot.model,
        accumulatedMs: lastAccMs,  // 新增
      })
```

同样修改 `forkFromEvent` 中的 `session_resume` 事件。

- [ ] **Step 5: 验证编译通过**

Run: `cd cCli && npx tsc --noEmit`
Expected: 无新增错误

- [ ] **Step 6: 提交**

```bash
git add cCli/src/ui/useChat.ts cCli/src/persistence/session-store.ts
git commit -m "feat(useChat): resume 时提取 accumulatedMs 并写入 session_resume 事件"
```

---

### Task 5: useStatusBar Hook — 系统资源采集

**Files:**
- Create: `src/ui/useStatusBar.ts`
- Test: `tests/unit/useStatusBar.test.ts`

- [ ] **Step 1: 编写 useStatusBar hook**

```typescript
// src/ui/useStatusBar.ts

/**
 * useStatusBar — 状态栏数据采集 hook。
 *
 * 职责：
 * - 定时采集 MEM（process.memoryUsage().rss / os.totalmem()）和 CPU（process.cpuUsage() 差值采样）
 * - 每秒更新运行时间（accumulatedMs + 本次运行时长）
 * - 从外部获取 token/context 数据（事件驱动，由调用方传入）
 * - 输出 StatusBarData 供 StatusBar.tsx 渲染
 */

import { useState, useEffect, useRef } from 'react'
import { totalmem, cpus } from 'node:os'

/** 资源采样间隔 */
const RESOURCE_INTERVAL_MS = 3000
/** 运行时间刷新间隔 */
const ELAPSED_INTERVAL_MS = 1000

/** 进度条色阶阈值 */
export const THRESHOLD_WARNING = 60
export const THRESHOLD_CRITICAL = 85

/** 进度条默认宽度 */
export const BAR_WIDTH = 8

export interface StatusBarData {
  memPercent: number
  memUsedBytes: number
  memTotalBytes: number
  cpuPercent: number
  cpuCoreCount: number
  elapsedMs: number
}

interface UseStatusBarOptions {
  /** 是否有消息（没消息时不采集） */
  hasMessages: boolean
  /** 历史累计时长（resume 带入） */
  accumulatedMs: number
  /** 本次 session 启动时间戳 */
  sessionStartTime: number
}

export function useStatusBar(options: UseStatusBarOptions): StatusBarData | null {
  const { hasMessages, accumulatedMs, sessionStartTime } = options

  const [data, setData] = useState<StatusBarData | null>(null)
  const prevCpuUsageRef = useRef<NodeJS.CpuUsage | null>(null)
  const prevCpuTimeRef = useRef<number>(0)

  // 系统常量（进程生命周期内不变）
  const totalMemRef = useRef(totalmem())
  const cpuCountRef = useRef(cpus().length)

  // ── 资源采样（MEM + CPU），每 3 秒 ──
  useEffect(() => {
    if (!hasMessages) return

    const sample = () => {
      const rss = process.memoryUsage().rss
      const totalMem = totalMemRef.current
      const memPercent = totalMem > 0 ? (rss / totalMem) * 100 : 0

      let cpuPercent = 0
      const now = Date.now()
      const currentCpu = process.cpuUsage()

      if (prevCpuUsageRef.current && prevCpuTimeRef.current > 0) {
        const elapsedMs = now - prevCpuTimeRef.current
        if (elapsedMs > 0) {
          const userDelta = currentCpu.user - prevCpuUsageRef.current.user
          const systemDelta = currentCpu.system - prevCpuUsageRef.current.system
          const totalCpuUs = userDelta + systemDelta
          const elapsedUs = elapsedMs * 1000
          cpuPercent = (totalCpuUs / elapsedUs / cpuCountRef.current) * 100
        }
      }

      prevCpuUsageRef.current = currentCpu
      prevCpuTimeRef.current = now

      setData(prev => {
        const elapsed = accumulatedMs + (Date.now() - sessionStartTime)
        const next: StatusBarData = {
          memPercent,
          memUsedBytes: rss,
          memTotalBytes: totalMem,
          cpuPercent: Math.min(100, Math.max(0, cpuPercent)),
          cpuCoreCount: cpuCountRef.current,
          elapsedMs: elapsed,
        }
        // 避免无意义重渲染：比较关键字段
        if (prev &&
            Math.abs(prev.memPercent - next.memPercent) < 0.1 &&
            Math.abs(prev.cpuPercent - next.cpuPercent) < 0.1 &&
            prev.elapsedMs === next.elapsedMs) {
          return prev
        }
        return next
      })
    }

    // 立即采样一次
    sample()
    const id = setInterval(sample, RESOURCE_INTERVAL_MS)
    return () => clearInterval(id)
  }, [hasMessages, accumulatedMs, sessionStartTime])

  // ── 运行时间，每 1 秒 ──
  useEffect(() => {
    if (!hasMessages) return

    const tick = () => {
      setData(prev => {
        if (!prev) return prev
        return { ...prev, elapsedMs: accumulatedMs + (Date.now() - sessionStartTime) }
      })
    }

    const id = setInterval(tick, ELAPSED_INTERVAL_MS)
    return () => clearInterval(id)
  }, [hasMessages, accumulatedMs, sessionStartTime])

  if (!hasMessages) return null
  return data
}
```

- [ ] **Step 2: 编写单元测试**

```typescript
// tests/unit/useStatusBar.test.ts
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'

// useStatusBar 是 React hook，完整测试需要 Ink 的 test renderer
// 这里测试导出的常量和辅助逻辑
import { THRESHOLD_WARNING, THRESHOLD_CRITICAL, BAR_WIDTH } from '../../src/ui/useStatusBar.js'

describe('useStatusBar 常量', () => {
  it('色阶阈值合理', () => {
    expect(THRESHOLD_WARNING).toBe(60)
    expect(THRESHOLD_CRITICAL).toBe(85)
    expect(THRESHOLD_WARNING).toBeLessThan(THRESHOLD_CRITICAL)
  })

  it('进度条默认宽度在范围内', () => {
    expect(BAR_WIDTH).toBeGreaterThanOrEqual(4)
    expect(BAR_WIDTH).toBeLessThanOrEqual(16)
  })
})
```

- [ ] **Step 3: 运行测试**

Run: `cd cCli && npx vitest run tests/unit/useStatusBar.test.ts`
Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add cCli/src/ui/useStatusBar.ts cCli/tests/unit/useStatusBar.test.ts
git commit -m "feat(ui): 新增 useStatusBar hook，采集 MEM/CPU/运行时间"
```

---

### Task 6: StatusBar.tsx — 纯展示组件

**Files:**
- Create: `src/ui/StatusBar.tsx`
- Test: `tests/unit/StatusBar.test.ts`

- [ ] **Step 1: 编写 StatusBar 组件**

```tsx
// src/ui/StatusBar.tsx

/**
 * StatusBar — 统一底部状态栏。
 *
 * InputBar 下方固定一行，展示系统资源 + token/context/cost。
 * 纯展示组件，所有数据由 props 注入。
 */

import React from 'react'
import { Box, Text } from 'ink'
import type { StatusBarData } from './useStatusBar.js'
import { BAR_WIDTH, THRESHOLD_WARNING, THRESHOLD_CRITICAL } from './useStatusBar.js'
import type { SessionCostStats } from '@observability/token-meter.js'
import type { ContextWindowState } from '@core/context-tracker.js'

interface StatusBarProps {
  data: StatusBarData | null
  tokenStats: SessionCostStats | null
  contextState: ContextWindowState | null
  terminalWidth: number
}

/** 渲染进度条 */
function renderBar(percent: number, width: number): string {
  const filled = Math.round((percent / 100) * width)
  const empty = width - filled
  return '█'.repeat(filled) + '░'.repeat(empty)
}

/** 根据百分比返回色阶颜色 */
function barColor(percent: number): string | undefined {
  if (percent >= THRESHOLD_CRITICAL) return 'red'
  if (percent >= THRESHOLD_WARNING) return 'yellow'
  return 'green'
}

/** 格式化字节为人类可读 */
function formatBytes(bytes: number): string {
  if (bytes >= 1024 * 1024 * 1024) {
    return `${(bytes / (1024 * 1024 * 1024)).toFixed(1)}GB`
  }
  return `${Math.round(bytes / (1024 * 1024))}MB`
}

/** 格式化毫秒为 MM:SS 或 HH:MM:SS */
function formatElapsed(ms: number): string {
  const totalSeconds = Math.floor(ms / 1000)
  const hours = Math.floor(totalSeconds / 3600)
  const minutes = Math.floor((totalSeconds % 3600) / 60)
  const seconds = totalSeconds % 60
  const pad = (n: number) => String(n).padStart(2, '0')
  if (hours > 0) {
    return `${pad(hours)}:${pad(minutes)}:${pad(seconds)}`
  }
  return `${pad(minutes)}:${pad(seconds)}`
}

/** 格式化 token 数值（K/M 缩写） */
function formatTokenCount(n: number): string {
  if (n >= 1_000_000) return `${(n / 1_000_000).toFixed(1)}M`
  if (n >= 1000) return `${(n / 1000).toFixed(1)}K`
  return String(n)
}

/** 各指标段的渲染定义 */
interface Segment {
  key: string
  width: number             // 预估纯文本宽度（用于截断判断）
  render: () => React.ReactNode
}

function buildSegments(
  data: StatusBarData,
  tokenStats: SessionCostStats | null,
  contextState: ContextWindowState | null,
): Segment[] {
  const segments: Segment[] = []

  // MEM
  const memBar = renderBar(data.memPercent, BAR_WIDTH)
  const memPct = `${Math.round(data.memPercent)}%`
  const memAbs = formatBytes(data.memUsedBytes)
  const memText = `MEM ${memBar} ${memPct} ${memAbs}`
  segments.push({
    key: 'mem',
    width: memText.length,
    render: () => (
      <Text>
        <Text dimColor>MEM </Text>
        <Text color={barColor(data.memPercent)}>{memBar}</Text>
        <Text dimColor> {memPct} {memAbs}</Text>
      </Text>
    ),
  })

  // CPU
  const cpuBar = renderBar(data.cpuPercent, BAR_WIDTH)
  const cpuPct = `${Math.round(data.cpuPercent)}%`
  const cpuText = `CPU ${cpuBar} ${cpuPct}`
  segments.push({
    key: 'cpu',
    width: cpuText.length,
    render: () => (
      <Text>
        <Text dimColor>CPU </Text>
        <Text color={barColor(data.cpuPercent)}>{cpuBar}</Text>
        <Text dimColor> {cpuPct}</Text>
      </Text>
    ),
  })

  // Elapsed
  const elapsed = formatElapsed(data.elapsedMs)
  const elapsedText = `⏱ ${elapsed}`
  segments.push({
    key: 'elapsed',
    width: elapsedText.length + 1,  // ⏱ 占 2 字符宽
    render: () => <Text dimColor>⏱ {elapsed}</Text>,
  })

  // Token（仅在有调用时显示）
  if (tokenStats && tokenStats.callCount > 0) {
    const tokIn = formatTokenCount(tokenStats.totalInputTokens)
    const tokOut = formatTokenCount(tokenStats.totalOutputTokens)
    const tokText = `${tokIn}/${tokOut} tok`
    segments.push({
      key: 'token',
      width: tokText.length,
      render: () => <Text dimColor>{tokText}</Text>,
    })
  }

  // Context（仅在有数据时显示）
  if (contextState && contextState.lastInputTokens > 0) {
    const ctxPct = `${(contextState.usedPercentage * 100).toFixed(0)}%`
    const ctxText = `Ctx ${ctxPct}`
    const ctxColor = contextState.level === 'overflow' || contextState.level === 'critical'
      ? 'red'
      : contextState.level === 'warning'
        ? 'yellow'
        : undefined
    segments.push({
      key: 'context',
      width: ctxText.length,
      render: () => (
        <Text {...(ctxColor ? { color: ctxColor } : { dimColor: true })}>
          {ctxText}
        </Text>
      ),
    })
  }

  // Cost（仅在有费用时显示）
  if (tokenStats && tokenStats.callCount > 0) {
    const sym = (c: string) => c === 'CNY' ? '¥' : '$'
    const costParts = Object.entries(tokenStats.costByCurrency)
      .filter(([, v]) => v > 0)
      .map(([c, v]) => `${sym(c)}${v.toFixed(4)}`)
    if (costParts.length > 0) {
      const costText = costParts.join('+')
      segments.push({
        key: 'cost',
        width: costText.length,
        render: () => <Text dimColor>{costText}</Text>,
      })
    }
  }

  return segments
}

export function StatusBar({ data, tokenStats, contextState, terminalWidth }: StatusBarProps): React.ReactNode {
  if (!data) return null

  const segments = buildSegments(data, tokenStats, contextState)
  const separator = ' | '
  const separatorWidth = separator.length

  // 窄终端截断：从右向左丢弃，直到总宽度 <= terminalWidth - 2（左右各留 1 列 padding）
  const maxWidth = terminalWidth - 2
  const visible: Segment[] = []
  let totalWidth = 0

  for (let i = 0; i < segments.length; i++) {
    const segWidth = segments[i].width + (i > 0 ? separatorWidth : 0)
    if (totalWidth + segWidth > maxWidth) break
    visible.push(segments[i])
    totalWidth += segWidth
  }

  if (visible.length === 0) return null

  return (
    <Box paddingX={1}>
      {visible.map((seg, i) => (
        <React.Fragment key={seg.key}>
          {i > 0 && <Text dimColor>{separator}</Text>}
          {seg.render()}
        </React.Fragment>
      ))}
    </Box>
  )
}
```

- [ ] **Step 2: 编写 formatElapsed 和 formatBytes 的单元测试**

为了可测试性，把 `formatElapsed`、`formatBytes`、`formatTokenCount`、`renderBar`、`barColor` 导出：

在 `StatusBar.tsx` 文件底部添加：

```typescript
// 导出供测试使用
export { renderBar, barColor, formatBytes, formatElapsed, formatTokenCount }
```

测试文件：

```typescript
// tests/unit/StatusBar.test.ts
import { describe, it, expect } from 'vitest'
import { renderBar, barColor, formatBytes, formatElapsed, formatTokenCount } from '../../src/ui/StatusBar.js'

describe('renderBar', () => {
  it('0% 全空', () => {
    expect(renderBar(0, 8)).toBe('░░░░░░░░')
  })

  it('100% 全满', () => {
    expect(renderBar(100, 8)).toBe('████████')
  })

  it('50% 半满', () => {
    expect(renderBar(50, 8)).toBe('████░░░░')
  })
})

describe('barColor', () => {
  it('低于 60% 绿色', () => {
    expect(barColor(30)).toBe('green')
    expect(barColor(59)).toBe('green')
  })

  it('60-85% 黄色', () => {
    expect(barColor(60)).toBe('yellow')
    expect(barColor(84)).toBe('yellow')
  })

  it('85%+ 红色', () => {
    expect(barColor(85)).toBe('red')
    expect(barColor(100)).toBe('red')
  })
})

describe('formatBytes', () => {
  it('MB 范围', () => {
    expect(formatBytes(256 * 1024 * 1024)).toBe('256MB')
  })

  it('GB 范围', () => {
    expect(formatBytes(2.5 * 1024 * 1024 * 1024)).toBe('2.5GB')
  })
})

describe('formatElapsed', () => {
  it('不足 1 小时显示 MM:SS', () => {
    expect(formatElapsed(65_000)).toBe('01:05')
  })

  it('超过 1 小时显示 HH:MM:SS', () => {
    expect(formatElapsed(3661_000)).toBe('01:01:01')
  })

  it('0 毫秒', () => {
    expect(formatElapsed(0)).toBe('00:00')
  })
})

describe('formatTokenCount', () => {
  it('小于 1000 原样输出', () => {
    expect(formatTokenCount(500)).toBe('500')
  })

  it('K 缩写', () => {
    expect(formatTokenCount(1500)).toBe('1.5K')
  })

  it('M 缩写', () => {
    expect(formatTokenCount(2_500_000)).toBe('2.5M')
  })
})
```

- [ ] **Step 3: 运行测试**

Run: `cd cCli && npx vitest run tests/unit/StatusBar.test.ts`
Expected: 全部 PASS

- [ ] **Step 4: 提交**

```bash
git add cCli/src/ui/StatusBar.tsx cCli/tests/unit/StatusBar.test.ts
git commit -m "feat(ui): 新增 StatusBar 组件及格式化函数单元测试"
```

---

### Task 7: App.tsx 集成 — 移除旧展示 + 挂载 StatusBar

**Files:**
- Modify: `src/ui/App.tsx`

- [ ] **Step 1: 添加 import**

在 App.tsx 顶部 import 区域添加：

```typescript
import { StatusBar } from './StatusBar.js'
import { useStatusBar } from './useStatusBar.js'
```

- [ ] **Step 2: 在 App 组件中调用 useStatusBar**

在 App 函数内部（现有 hook 调用附近），添加：

```typescript
  const config = useMemo(() => configManager.load(), [])
  const statusBarEnabled = config.statusBar !== false  // 缺省 true

  const statusBarData = useStatusBar({
    hasMessages: statusBarEnabled && (messages.length > 0 || isStreaming),
    accumulatedMs,
    sessionStartTime,
  })
```

其中 `accumulatedMs` 和 `sessionStartTime` 来自 useChat 返回值，需要在解构处添加：

```typescript
  const {
    // ... 现有字段
    accumulatedMs,
    sessionStartTime,
  } = useChat(...)
```

- [ ] **Step 3: 移除旧的 token/context/cost 内联渲染**

删除 App.tsx 中约 916-940 行的这段代码：

```tsx
          {started && !isStreaming && (() => {
            const s = tokenMeter.getSessionStats()
            if (s.callCount === 0) return null
            const fmt = (n: number) => n >= 1000 ? (n / 1000).toFixed(1) + 'K' : String(n)
            const sym = (c: string) => c === 'CNY' ? '¥' : '$'
            const costParts = Object.entries(s.costByCurrency).filter(([, v]) => v > 0).map(([c, v]) => `${sym(c)}${v.toFixed(4)}`)
            const cost = costParts.length > 0 ? ` | ${costParts.join(' + ')}` : ''
            const ctxInfo = contextState
              ? ` | Context: ${(contextState.usedPercentage * 100).toFixed(0)}%`
              : ''
            const ctxColor = contextState?.level === 'overflow' ? 'red'
              : contextState?.level === 'critical' ? 'red'
              : contextState?.level === 'warning' ? 'yellow'
              : undefined
            return (
              <Box paddingX={1}>
                <Text dimColor>{fmt(s.totalInputTokens)} in / {fmt(s.totalOutputTokens)} out{cost}</Text>
                {contextState && (
                  <Text {...(ctxColor ? { color: ctxColor } : { dimColor: true })}>
                    {ctxInfo}
                  </Text>
                )}
              </Box>
            )
          })()}
```

- [ ] **Step 4: 在 InputBar 下方插入 StatusBar**

在 InputBar 组件之后、`CommandSuggestion` / `AtSuggestion` 之前，插入：

```tsx
          <InputBar
            key={inputResetKey}
            value={inputValue}
            onChange={setInputValue}
            onSubmit={handleSubmit}
            onInterruptSubmit={(text) => {
              setInputValue('')
              interruptAndSubmit(text)
            }}
            streaming={isStreaming}
            history={userHistory}
          />
          {statusBarEnabled && (
            <StatusBar
              data={statusBarData}
              tokenStats={started ? tokenMeter.getSessionStats() : null}
              contextState={contextState}
              terminalWidth={terminalSize.columns}
            />
          )}
```

注意 `terminalSize` 来自现有的 `useTerminalSize()` hook，App.tsx 中已有。

- [ ] **Step 5: 验证编译通过**

Run: `cd cCli && npx tsc --noEmit`
Expected: 无新增错误

- [ ] **Step 6: 手动验证**

Run: `cd cCli && node dist/bin/ccli.js`

验证：
1. 启动后进入 WelcomeScreen，底部无状态栏
2. 发送一条消息后，InputBar 下方出现状态栏
3. 状态栏显示 MEM 进度条 + CPU 进度条 + 运行时间
4. LLM 响应完成后，状态栏追加 token/context/cost 信息
5. 运行时间每秒跳动

- [ ] **Step 7: 提交**

```bash
git add cCli/src/ui/App.tsx
git commit -m "feat(ui): App.tsx 集成 StatusBar，移除旧 token/context 内联展示"
```

---

### Task 8: config.statusBar=false 关闭验证

**Files:**
- 无新文件

- [ ] **Step 1: 手动验证关闭开关**

在 `~/.ccode/config.json` 中添加 `"statusBar": false`，重启 cCli。

验证：
1. 发送消息后底部无状态栏
2. token/context/cost 也不再显示（已移除旧代码，statusBar=false 时全部隐藏）
3. 不启动任何定时器（通过 `useStatusBar` 的 `hasMessages` 为 false 实现）

- [ ] **Step 2: 恢复 config 删除 statusBar 字段**

验证缺省情况下 `statusBar` 默认为 `true`。

- [ ] **Step 3: 提交（如有修复）**

如果验证过程中有修复，提交：

```bash
git add -u
git commit -m "fix(ui): StatusBar 开关与缺省值修复"
```

---

### Task 9: resume 跨会话时间累加验证

**Files:**
- 无新文件

- [ ] **Step 1: 启动新会话并记录时间**

1. 启动 cCli，发送一条消息
2. 观察状态栏运行时间开始计时
3. 等待约 30 秒，记录显示时间（如 `00:30`）
4. 退出 cCli

- [ ] **Step 2: resume 并验证时间延续**

1. `ccode --resume` 选择刚才的会话
2. 验证状态栏运行时间从约 `00:30` 开始（不是从 `00:00`）
3. 继续等待，确认时间正常累加

- [ ] **Step 3: 检查 JSONL**

查看 session JSONL 文件：
- `session_end` 事件包含 `accumulatedMs` 字段
- `session_resume` 事件包含 `accumulatedMs` 字段
- 值合理（约等于实际运行秒数 × 1000）

---

## 实现顺序总结

```
Task 1 (config)           ── 无依赖
Task 2 (session-types)    ── 无依赖
Task 3 (session-logger)   ── 依赖 Task 2
Task 4 (useChat + store)  ── 依赖 Task 2, 3
Task 5 (useStatusBar)     ── 无依赖
Task 6 (StatusBar.tsx)    ── 依赖 Task 5
Task 7 (App.tsx 集成)     ── 依赖 Task 1, 4, 5, 6
Task 8 (关闭验证)         ── 依赖 Task 7
Task 9 (resume 验证)      ── 依赖 Task 7
```

可并行的任务组：
- **组 A**：Task 1 + Task 2 + Task 5（互不依赖）
- **组 B**：Task 3 → Task 4（串行）
- **组 C**：Task 6（依赖 Task 5）
- **集成**：Task 7（汇总全部）
- **验证**：Task 8 + Task 9（并行）
